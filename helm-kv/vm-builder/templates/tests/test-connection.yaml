apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vm-builder.fullname" . }}-test-connection"
  labels:
{{ include "vm-builder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vm-builder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
