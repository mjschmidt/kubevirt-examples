apiVersion: v1
kind: Service
metadata:
  name: {{ include "vm-builder.fullname" . }}
  labels:
    {{- include "vm-builder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vm-builder.selectorLabels" . | nindent 4 }}
