apiVersion: v1
kind: Service
metadata:
  name: {{ include "pvc-builder.fullname" . }}
  labels:
    {{- include "pvc-builder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pvc-builder.selectorLabels" . | nindent 4 }}
