apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pvc-builder.fullname" . }}-test-connection"
  labels:
{{ include "pvc-builder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pvc-builder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
