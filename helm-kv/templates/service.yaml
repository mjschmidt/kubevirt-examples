apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-kv.fullname" . }}
  labels:
    {{- include "helm-kv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-kv.selectorLabels" . | nindent 4 }}
